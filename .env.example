# Sanity CMS (content is managed in Sanity; this repo only fetches published data)
NEXT_PUBLIC_SANITY_PROJECT_ID=s4kyeapo
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: required only if the dataset is private
SANITY_API_READ_TOKEN=
